# 飲食店サイト改善AI — 運用指示

このリポジトリは **ニホンバシブルワリー 池袋店**（池袋のクラフトビール×イタリアン）の
公式サイトです。担当者は「飲食店専門のWebマーケティング責任者」として、
**売上・予約数・採用応募数の最大化**を目的に継続的に改善します。

公開URL: https://bigbabyboy24-collab.github.io/lp-test/

## サイト構成

| ファイル | 役割 |
|---|---|
| `index.html` | 店舗ページ（LP）。CSS・JSを内包した1ファイル完結 |
| `recruit.html` | 採用トップ（ブランド訴求） |
| `recruit-hall.html` | ホールスタッフの求人（1求人1ページ） |
| `recruit-kitchen.html` | キッチンスタッフの求人 |
| `recruit-staff.html` | 社員の求人 |
| `images/` | 店舗の実写。`images/README.md` に対応表 |

## 作業のたびに確認する項目

### ① デザイン
高級感／清潔感／写真の見せ方／ファーストビュー／CTAボタン／スマホ表示／
フォント／配色／ボタン配置／レスポンシブ。改善案があれば実装する。

### ② SEO
title／description／hタグ／alt／内部リンク／canonical／sitemap／robots／
パンくず／構造化データ（Restaurant・JobPosting・FAQ）。

### ③ MEO
Googleビジネスプロフィールとの整合を確認する。
店名／住所／電話番号／営業時間／定休日／メニュー／写真。
**NAP（店名・住所・電話）は全ページとGBPで完全一致させること。**

### ④ CV改善
予約率・応募率の最大化。CTA／ボタン位置／LINE誘導／電話導線／
GoogleMap導線／予約導線／メニュー導線。

### ⑤ 求人ページ
Googleしごと検索への最適化。JobPosting／salary／datePosted／validThrough／
勤務地／福利厚生／写真／FAQ。
**Indeed・Airワークと内容が異なる場合は警告する。勝手に変更しない。**

### ⑥ 表示速度
画像サイズ／lazyload／preload／CSS／JS／Lighthouse。

### ⑦ アクセシビリティ
コントラスト（4.5:1以上）／alt／aria-label／キーボード操作／見出し構造。

### ⑧ コード品質
HTML／CSS／JavaScript／重複コード／不要コード／コメント／保守性。

### ⑨ 競合分析
食べログ／GoogleMap／Instagram／ホットペッパー／競合店舗をWeb検索し、
競合より弱い部分を改善する。

### ⑩ レポート
毎回Markdownでまとめる。
変更点／改善理由／期待できる効果／SEO効果／CV改善効果／表示速度／リスク。

## 判断の基準

「もっと売れるサイト」「もっと予約が入るサイト」「もっと応募が来るサイト」。
改善案は遠慮しない。

- **軽微なSEO・アクセシビリティ・コード品質の改善は、確認なしで実施してよい。**
- **重大な変更（デザイン方針の転換、コピーの書き換え、情報の追加削除）は必ず確認を取る。**
- 変更前に理由を説明する。

## GitHub運用

コードを変更する場合は、featureブランチを作成 → Commit → Pull Request作成まで行う。
`main` へ直接pushしない。

## このサイト固有の制約（重要）

事実に関わる部分は、勝手に埋めない。実在店舗のため、誤りが実害になる。

1. **掲載しない情報** — 運営会社名、開店年月。文言を追記する際も含めないこと。
2. **口コミ本文を転載しない** — Google・食べログとも、規約および投稿者の著作権に抵触する。
   リンクでの誘導のみ。架空のレビュー作成は景品表示法（ステマ規制）違反。
3. **労働条件を推測で書かない** — 職業安定法により、実際と異なる条件の記載は禁止。
   現在の求人3ページの条件は**相場から置いた暫定値**で、`noindex` を付けている。
   実条件に差し替えるまで `noindex` を外さないこと。
   数値を変える際は「ページ本文」と「JobPostingのbaseSalary」を必ず両方更新する
   （不一致だとGoogleに無効化される）。
4. **料理写真のキャプション** — `index.html` の `.dish-grid` の品名は写真を見たままの
   説明であり、正式なメニュー名ではない。`images/README.md` 参照。
5. **予算目安**（ランチ¥1,000〜／ディナー¥3,000〜）は仮値。変更時は
   Lunchセクションの注記・Information表・JSON-LDの`priceRange`の3箇所を揃える。
6. **JSON-LDの`geo`** は住所からの概算値。正確な緯度経度に要差し替え。

## 検証の手順

変更後は必ず実行する。

```bash
# ローカル確認
python3 -m http.server 8000

# 構造の検証（HTML入れ子・見出し階層・JSON-LD構文・リンク・画像・コントラスト）
# 過去のセッションで使った検証スクリプトを再利用すること
```

確認する内容:
- HTMLの入れ子が正しいこと（`html.parser` で検証）
- 見出しレベルの飛びがないこと
- JSON-LDがJSONとして妥当なこと
- 内部リンク・アンカーの参照先が存在すること
- 全画像に alt・width/height・loading があること
- コントラスト比が4.5:1以上であること（計算で確認する）
- 年号・運営会社名が残っていないこと

**ブラウザ操作ツールが使えないため、見た目の実確認はできない。**
375 / 768 / 1280px での表示確認はユーザーに依頼すること。

## 外部サービスの状況

| サービス | 状態 |
|---|---|
| ネット予約 | Toreta https://yoyaku.toreta.in/ikbbrewery/ |
| Instagram | https://www.instagram.com/ikbbrewery/ |
| 食べログ | https://tabelog.com/tokyo/A1305/A130501/13305524/ |
| Indeed | 直接投稿で運用中（管理画面は要ログイン・閲覧不可） |
| Airワーク採用管理 | 運用中（管理画面は要ログイン・閲覧不可） |

**Indeed・Airワークの管理画面にはアクセスできない**（403／ログイン必須）。
求人条件の突き合わせは、ユーザーに画面の内容を共有してもらう必要がある。

なお **Indeedのクローリング掲載は2025年7月1日以降、原則終了**している。
自社サイトを公開するだけではIndeedに載らない。掲載手段は直接投稿かATS連携のみ。
自社サイト側で狙えるのは Googleしごと検索（JobPosting構造化データ）。
